import { searchResultsProps } from "@/features/_disciplines";
import { SearchResultsPage } from "@/features/search";

export default function AcademicSearchResultsPage() {
  return <SearchResultsPage {...searchResultsProps} />;
}